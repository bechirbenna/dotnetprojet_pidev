@model IEnumerable<data.recommendation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdv.cshtml";
}


<div class="content-wrapper">
    <div class="content-heading">
        <div>
            Recommendation<small></small>
        </div>
    </div>
    <p><div class="btn btn-light">
    @Html.ActionLink("Create new recommendation", "Create")
</div>
    </p>
    <table class="table">
        <tr>
            <th>
                Description 
            </th>
            <th>
                Date
            </th>
            <th></th>
        </tr>

        @foreach (var item in ViewBag.result)
        {
            <tr>
                <td>
                    @item.recId
                </td>
                <td>
                    @item.recDesc
                </td>
                <td>
                    @{ long recId = @item.recId;}
                    <a href="@Url.Action("Edit", "Recommendation",new {id=@item.recId})" class="btn btn-success">Edit</a>
                    @{ long reccId = @item.recId;}
                    <a href="@Url.Action("Delete", "Recommendation",new {id=@item.recId})" class="btn btn-danger">Delete</a>
                </td>
            </tr>
                        }

    </table>
</div>